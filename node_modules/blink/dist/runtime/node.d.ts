import { AgentDefaultExport } from "../index-C29YiDK4.js";
import * as _whatwg_node_server0 from "@whatwg-node/server";

//#region src/runtime/node.d.ts

/**
 * requireAsCommonJS loads and validates an agent from a given entrypoint.
 * This is a helper function - but the `AgentDefaultExport` can be used directly.
 *
 * @param entrypoint - The entrypoint of the agent to load.
 * @returns The loaded agent.
 */
declare function requireAsCommonJS(entrypoint: string): AgentDefaultExport;
/**
 * createHTTPHandler creates a HTTP handler for a given agent.
 * This can be passed directly to `http.createServer`.
 *
 * @param agent
 * @returns
 */
declare function createHTTPHandler(agent: AgentDefaultExport): _whatwg_node_server0.ServerAdapter<{}, _whatwg_node_server0.ServerAdapterBaseObject<{}, (request: Request) => Promise<Response> | Response>>;
//#endregion
export { createHTTPHandler, requireAsCommonJS };