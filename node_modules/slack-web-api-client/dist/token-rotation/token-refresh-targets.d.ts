export interface TokenRefreshTargets {
    bot?: {
        access_token: string;
        refresh_token: string;
        token_expires_at: number;
    };
    user?: {
        access_token: string;
        refresh_token: string;
        token_expires_at: number;
    };
}
//# sourceMappingURL=token-refresh-targets.d.ts.map