import type { TokenRotatorOptions } from "./token-rotator-options";
import type { TokenRefreshResults } from "./token-refresh-results";
import type { TokenRefreshTargets } from "./token-refresh-targets";
export declare class TokenRotator {
    #private;
    constructor(options: TokenRotatorOptions);
    performRotation(targets: TokenRefreshTargets): Promise<TokenRefreshResults>;
}
//# sourceMappingURL=token-rotator.d.ts.map